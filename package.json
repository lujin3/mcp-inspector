{
    "name": "mcp-inspector",
    "version": "0.0.9",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "tauri": "tauri"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.1.0"
    }
}